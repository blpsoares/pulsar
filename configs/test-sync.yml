command:
  sync:
    source:
      # Mongo local de teste (docker-compose-test.yml: mongo-a, replica set rs0).
      # NUNCA coloque credenciais reais aqui — este arquivo é versionado.
      uri: "mongodb://127.0.0.1:27020/?replicaSet=rs0&directConnection=true"
      db: "source-db"
    destination:
      uri: "mongodb://127.0.0.1:27021"
      db: "dest-db"
    collections: []
