{
  "version": "1.0.0",
  "name": "populate-fakedb",
  "module": "./src/app.js",
  "type": "module",
  "scripts": {
    "populate": "bun src/app.js",
    "up:db": "docker-compose  up -d",
    "down:db": "docker-compose down",
    "clean": "docker rm -f $(docker ps -a -q) && docker rmi -f populate-fakedb_mongo"
  },
  "dependencies": {
    "mongodb": "6.9.0",
    "chance": "^1.1.8",
    "cli-progress": "^3.10.1"
  },
  "devDependencies": {
    "@types/bun": "1.1.9"
  }
}
